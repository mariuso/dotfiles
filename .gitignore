.DS_Store
BREWFILE
BREWFILE.lock.json
BrewfileCurrent

# Exclude the actual dnsmasq.sh file (keep only the template)
install/dnsmasq.sh

# Exclude custom dnsmasq domains (keep only the example)
dnsmasq/domains.txt
# Private GCloud project aliases and the starship config generated from them
starship/starship_gcloud_aliases.toml
starship/starship.local.toml

# Local Claude Code settings
.claude/settings.local.json
