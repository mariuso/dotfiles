# dnsmasq custom domains
# Add one domain per line
# Lines starting with # are ignored

example.dev
myapp.local
api.test

# You can add more domains below:
# staging.dev
# backend.local
# Optionally give an IP (default 127.0.0.1), e.g. a remote dev VM on the tailnet:
# myapp.test 100.x.y.z
